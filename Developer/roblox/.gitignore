*.rbxlx.lock
*.rbxl.lock

# bv (beads viewer) local config and caches
.bv/
.sagebrook_env

# Agent Mail session credentials (contains live registration token)
.agent_mail_env

# python caches from scripts/
__pycache__/

# e2e test logs (generated by tests/e2e/TestLogger.lua)
testlogs/

# rojo test build output (regenerated by `rojo build test.project.json`)
HarborHeist_tests.rbxlx

# rojo scenario-e2e build output (regenerated by scripts/run_e2e_scenarios.sh)
HarborHeist_scenarios.rbxlx

# Abandoned E2E test-bridge debug artifacts (superseded by the
# _G.HARBORHEIST_TEST bridge in src/server/init.server.lua — ModuleScripts
# are per-instance isolated, so the TestBridge module approach was dropped;
# the *.server.lua scripts were one-off diagnostics from that debugging).
# Kept locally for reference only — never commit.
src/server/TestBridge.lua
test_bridge_check.server.lua
test_bridge_debug.server.lua
test_bridge_timing.server.lua
